export default function PageHeaders() {
    return (
        <section className="text-center mt-24 mb-8">
            <h1 className="text-3xl" style={{ textShadow: '3px 3px 0 rgba(0, 0, 0, .2)' }}>
                Add captions to your videos!
            </h1>
            <h2 className="text-white/80">
                Just upload your video and we will do the rest!
            </h2>
        </section>
    );
}